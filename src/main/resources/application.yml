spring:
  application:
    name: backend-service
  jpa:
    open-in-view: false
  flyway:
    locations: classpath:db/migration

server:
  port: 9000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
