spring:
  application:
    name: backend-service
  jpa:
    open-in-view: false
  flyway:
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

server:
  port: 9000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
